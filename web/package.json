{
  "name": "ayo-web",
  "private": true,
  "type": "module",
  "description": "Front Ayo — modules ES natifs, sans build ni dépendance. Ce fichier sert uniquement à ce que Node interprète les .js comme des modules ES lors des tests (le navigateur le sait déjà grâce à <script type=\"module\">).",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
